type TimerTendProps = {
    title: string;
    defaultQuantity: number;
    targetQuantity: number;
    resetTime: Date;
};

export default function TimerTend({
    title,
    defaultQuantity,
    targetQuantity,
    resetTime,
}: TimerTendProps) {
    return <div></div>;
}
