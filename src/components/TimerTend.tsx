export type TimerTendProps = {
    title: string;
    startTime: Date;
    targetTime: Date;
};

export default function TimerTend({
    title,
    startTime,
    targetTime,
}: TimerTendProps) {
    return <div></div>;
}
